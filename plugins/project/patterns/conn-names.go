package patterns

// data sources (sub-keys) flags
const (
	SourceNamePostgres = "postgres"
	SourceNameRedis    = "redis"
)

// server type flags
const (
	RESTHTTPServer = "rest"
	GRPCServer     = "grpc"
	TelegramServer = "telegram"
)

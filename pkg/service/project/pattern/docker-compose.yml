version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '10100:8080'
    restart: unless-stopped
    volumes:
      - './configs/dev.yaml:/app/configs/dev.yaml'
    entrypoint: './main'